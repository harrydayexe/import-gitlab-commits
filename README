# Import GitLab Commits

A tool to import GitLab commits from private GitLab instances to a separate repo. This tool is based on the tool of the same name by [alexandear](https://github.com/alexandear/import-gitlab-commits) except written in NodeJS (because my organisation does not allow Go.
